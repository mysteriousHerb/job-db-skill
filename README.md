# job-db

Utilities for Notion-backed job tracking.

- `push.py`: push scored jobs from `job-scout` into Notion.
- `pull.py`: pull tracked jobs from Notion back into `cv_builder` input files.
- `filter_notion_duplicates.py`: remove jobs already tracked in Notion before scoring.
